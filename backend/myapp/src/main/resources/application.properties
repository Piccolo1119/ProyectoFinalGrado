spring.application.name=myapp
server.port=8080

# Configuración de la conexión a la base de datos PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/WEB_GUIT_BASS
spring.datasource.username=postgres
spring.datasource.password=admin

# Configuración del dialecto de Hibernate para PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuración del esquema de la base de datos (opcional)
spring.datasource.schema=public

logging.level.org.springframework.security=TRACE.